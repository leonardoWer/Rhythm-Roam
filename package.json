{
  "name": "rhythm-roam",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "gsap": "^3.13.0",
    "three": "^0.179.1"
  }
}
